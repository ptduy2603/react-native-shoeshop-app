import { View, Text, StyleSheet } from 'react-native'

function Cart() {
    return ( 
        <View>
            <Text>cart screen</Text>
        </View>
    )
}

const styles = StyleSheet.create({})

export default Cart;